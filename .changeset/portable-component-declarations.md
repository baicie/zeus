---
'@zeus-js/component-analyzer': patch
'@zeus-js/component-dts': patch
---

Generate self-contained component declarations for portable local prop types and
fail closed when source-bound or unsupported TypeScript types cannot be emitted
safely for downstream consumers.
