---
'@zeus-js/bundler-plugin': minor
'@zeus-js/output-react-wrapper': patch
---

Add Rollup and Rolldown bundler entry points with low-configuration TS/TSX handling, config helpers, Rolldown support, component plugin external merging, and updated TypeScript transpilation semantics.

Update generated React wrappers to use named React imports and direct `forwardRef`.
