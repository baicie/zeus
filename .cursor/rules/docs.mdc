# Zeus 框架文档规范

## 基本要求

- 提供中英文两个版本
- 新的属性需要声明可用的版本号
- 属性命名符合 Zeus 框架的 API 命名规则
- 文档结构清晰，便于用户快速查找和理解

## 文档结构

### 项目根目录文档

```
docs/
├── README.md              # 项目介绍
├── getting-started.md     # 快速开始
├── installation.md        # 安装指南
├── components/            # 组件文档
│   ├── button.md
│   ├── form.md
│   └── ...
├── api/                   # API 文档
│   ├── core.md
│   ├── components.md
│   └── ...
└── examples/              # 示例代码
    ├── basic/
    ├── advanced/
    └── ...
```

### 组件文档结构

每个组件文档应包含：

1. **组件介绍**：用途、特点、适用场景
2. **基础用法**：最简单的使用示例
3. **进阶用法**：复杂场景的示例
4. **API 说明**：完整的属性列表
5. **设计指南**：设计原则和最佳实践

## Changelog 规范

### 文件组织

- `CHANGELOG.md`：英文版本
- `CHANGELOG.zh-CN.md`：中文版本
- 每个版本一个区块，按时间倒序排列

### 版本格式

```markdown
## [版本号] - YYYY-MM-DD

### 🆕 新特性

- 新增功能描述

### 🐞 Bug 修复

- 修复问题描述

### 💄 样式更新

- 样式变更描述

### 📝 文档改进

- 文档更新描述
```

### 变更类型

- 🆕 新特性：新增功能或组件
- 🐞 Bug 修复：修复已知问题
- 💄 样式更新：UI 样式或设计 token 更新
- 📝 文档改进：文档、示例或网站更新
- ✅ 测试更新：新增或更新测试用例
- 🛠 重构：代码重构或工具链优化
- ⚡️ 性能提升：性能优化
- 🗑 废弃：废弃功能或 API
- 🔥 重要更新：值得特别关注的重要变更

### 编写原则

- 从用户角度描述变更，而非技术实现
- 描述问题的影响，而非解决方案
- 避免技术术语，使用用户友好的语言
- 提供具体的示例或链接

### 示例

```markdown
## [1.2.0] - 2024-01-15

### 🆕 新特性

- 新增 `Button` 组件的 `loading` 状态支持
- 新增 `Form` 组件的异步验证功能

### 🐞 Bug 修复

- 修复了 `Table` 组件在 Safari 浏览器中的排序问题
- 修复了 `Modal` 组件在移动端的显示异常

### 💄 样式更新

- 更新了 `Button` 组件的默认样式，提升视觉一致性
- 优化了 `Input` 组件的聚焦状态样式
```

## API 文档规范

### 属性表格格式

```markdown
| Property   | Description | Type                                 | Default     | Version |
| ---------- | ----------- | ------------------------------------ | ----------- | ------- |
| `type`     | 按钮类型    | `'primary' \| 'default' \| 'dashed'` | `'default'` | 1.0.0   |
| `disabled` | 是否禁用    | `boolean`                            | `false`     | 1.0.0   |
| `loading`  | 加载状态    | `boolean`                            | `false`     | 1.2.0   |
```

### 文档要求

- 属性按字母顺序排列
- 类型定义使用 TypeScript 语法
- 默认值使用代码格式
- 新增属性标注版本号
- 描述简洁明了，首字母大写

### 代码示例

````markdown
### 基础用法

```tsx
import { Button } from '@zeus/components'

function App() {
  return (
    <Button type="primary" onClick={() => console.log('clicked')}>
      Click me
    </Button>
  )
}
```
````

### 加载状态

```tsx
import { Button } from '@zeus/components'

function App() {
  const [loading, setLoading] = useState(false)

  return (
    <Button
      loading={loading}
      onClick={() => {
        setLoading(true)
        // 异步操作
        setTimeout(() => setLoading(false), 2000)
      }}
    >
      Submit
    </Button>
  )
}
```

```

## 组件文档最佳实践

### 示例设计
- 提供完整可运行的代码示例
- 包含多种使用场景
- 展示组件的各种状态和变体
- 提供在线演示链接

### 交互式文档
- 使用 Storybook 或类似工具
- 支持在线编辑和预览
- 提供代码复制功能
- 支持主题切换

### 可访问性
- 包含可访问性指南
- 提供键盘导航说明
- 说明屏幕阅读器支持
- 提供 ARIA 属性使用建议

## 国际化文档

### 翻译原则
- 保持技术术语的一致性
- 适应不同语言的文化习惯
- 确保翻译的准确性和完整性
- 定期更新和维护翻译

### 文档同步
- 中英文文档同步更新
- 使用统一的文档模板
- 建立文档审查流程
- 收集用户反馈并改进

## 文档维护

### 版本控制
- 文档变更需要代码审查
- 与代码变更同步发布
- 维护文档的版本历史
- 定期清理过时内容

### 质量保证
- 定期检查文档的准确性
- 验证代码示例的可运行性
- 收集用户反馈并改进
- 建立文档质量指标

### 自动化
- 自动生成 API 文档
- 自动检查文档链接
- 自动生成示例代码
- 集成文档测试

- 对复杂功能提供详细说明
- 加入 TypeScript 定义
- 提供常见问题解答
- 更新文档时同步更新中英文版本
```
