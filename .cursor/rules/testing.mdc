# Zeus 框架测试规范

## 测试框架

- 使用 **Vitest** 作为主要测试框架
- 使用 **React Testing Library** 进行组件测试
- 使用 **jsdom** 提供 DOM 环境

## 测试覆盖率要求

- 单元测试覆盖率不低于 **80%**
- 核心功能模块覆盖率不低于 **90%**
- 公共 API 必须有完整的测试覆盖

## 测试文件组织

### 文件命名

- 测试文件命名格式：`ComponentName.test.tsx` 或 `ComponentName.spec.tsx`
- 工具函数测试：`utils.test.ts` 或 `utils.spec.ts`
- 测试文件放在 `__tests__` 目录或与源文件同级

### 目录结构

```
src/
├── components/
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.test.tsx
│   │   └── __tests__/
│   │       └── Button.integration.test.tsx
│   └── ...
└── utils/
    ├── helpers.ts
    └── helpers.test.ts
```

## 测试类型

### 单元测试

- 测试单个函数或组件的独立功能
- 使用 mock 隔离外部依赖
- 测试边界条件和异常情况

### 集成测试

- 测试组件间的交互
- 测试与外部服务的集成
- 验证完整的功能流程

### 快照测试

- 对 UI 组件使用快照测试
- 确保组件渲染结果的一致性
- 及时更新快照文件

## 测试编写规范

### 测试结构

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { Button } from './Button'

describe('Button Component', () => {
  it('should render with default props', () => {
    render(<Button>Click me</Button>)
    expect(screen.getByRole('button')).toBeInTheDocument()
  })

  it('should handle click events', () => {
    const handleClick = vi.fn()
    render(<Button onClick={handleClick}>Click me</Button>)

    screen.getByRole('button').click()
    expect(handleClick).toHaveBeenCalledTimes(1)
  })
})
```

### 测试命名

- 使用描述性的测试名称
- 遵循 "should" 或 "when" 的命名模式
- 清晰表达测试的预期行为

### Mock 使用

- 使用 `vi.fn()` 创建函数 mock
- 使用 `vi.mock()` 模拟模块
- 避免过度 mock，保持测试的真实性

## 测试最佳实践

### 可读性

- 使用 `describe` 块组织相关测试
- 使用 `beforeEach` 和 `afterEach` 设置测试环境
- 保持测试代码简洁明了

### 可靠性

- 避免测试间的相互依赖
- 使用 `vi.clearAllMocks()` 清理 mock 状态
- 确保测试的可重复性

### 性能

- 避免不必要的异步操作
- 合理使用 `vi.useFakeTimers()`
- 及时清理测试资源

## 持续集成

### CI 配置

- 在 CI 中运行完整的测试套件
- 生成测试覆盖率报告
- 设置测试失败时的构建阻断

### 测试命令

```bash
# 运行所有测试
pnpm test

# 运行单元测试
pnpm test:unit

# 运行测试并生成覆盖率报告
pnpm test:coverage

# 监听模式运行测试
pnpm test:watch
```

- 使用 Jest 和 React Testing Library 编写单元测试
- 对 UI 组件使用快照测试 (Snapshot Testing)
- 测试覆盖率要求 100%
- 测试文件放在 **tests** 目录，命名格式为：index.test.tsx 或 xxx.test.tsx
