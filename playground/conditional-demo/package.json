{
  "name": "zeus-conditional-demo",
  "version": "1.0.0",
  "description": "Zeus 条件表达式和列表渲染编译测试",
  "scripts": {
    "dev": "node ./build.mjs",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit --jsx react-jsx --esModuleInterop --moduleResolution node --target ES2020 --strict --skipLibCheck tsx/*.tsx"
  },
  "devDependencies": {
    "@zeus-js/compiler-core": "workspace:*",
    "@types/react": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
