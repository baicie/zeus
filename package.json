{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:zeus": "pnpm run build zeus",
    "build:core": "pnpm run build core",
    "build:shared": "pnpm run build shared",
    "dev": "turbo run dev",
    "clean": "turbo run clean"
  },
  "packageManager": "pnpm@10.6.1",
  "devDependencies": {
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-replace": "^5.0.5",
    "@types/node": "^22.13.10",
    "rollup-plugin-esbuild": "^6.1.1",
    "tsup": "^8.4.0",
    "turbo": "^2.4.4",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
