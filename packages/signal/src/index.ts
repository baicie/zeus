import {
  computed,
  effect,
  effectScope,
  isComputed,
  isEffect,
  isEffectScope,
  isSignal,
  signal,
  trigger,
} from 'alien-signals'

export {
  isComputed,
  isEffect,
  isEffectScope,
  isSignal,
  signal,
  computed,
  effect,
  effectScope,
  trigger,
}
