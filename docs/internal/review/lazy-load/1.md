## 结论

本次审查基于分支最新 head `44a677b4a60219324cc3f02e753c123f9def1135`。

**暂不建议合并。** 这版已经把上一轮的主要问题基本修完，整体完成度已经接近可合并：

- React minimal wrapper 已保留组件 props，并自动引入 WC bootstrap 模块。
- Vue minimal wrapper 已支持 named slots，并自动引入 WC bootstrap 模块。
- lazy 模式已经生成 `components.manifest.js`、`loader.js`、`auto.js`、兼容 wrapper 的虚拟模块和 lazy `index.js`。
- pre-upgrade property、`componentOnReady`、初始化失败状态已经有对应实现。

目前还剩两个应阻塞合并的问题。

---

# P0：lazy 组件断开重连后会丢失 light DOM slot，并可能破坏 closed shadow root

直接 `defineElement` 模式会把 `lightChildren` 和 `capturedLightChildren` 保存到元素实例上，只在首次连接时捕获一次。

但 `mountElementDefinition()` 每次执行都会重新从当前 `host.childNodes` 捕获 light children。
lazy entry 在断开时会 dispose 并清空 `mounted`，重连时重新调用 `mountElementDefinition()`。

这会导致：

```txt
首次连接：
  捕获原始 slot children
  render 清空 host 并重新插入内容

断开：
  dispose 清空渲染内容

重连：
  再次捕获 host.childNodes
  此时原始 slot children 已经没有了
```

此外，`resolveExternalRenderTarget()` 每次重新 mount 都通过 `host.shadowRoot ?? host.attachShadow(...)` 获取目标。对于 `mode: "closed"` 的 ShadowRoot，`host.shadowRoot` 始终为 `null`，重连时会尝试第二次 `attachShadow()`。

## 修改 `packages/core/runtime-dom/src/defineElement.ts`

新增持久化 mount state：

```ts
export interface ElementDefinitionMountState {
  target?: Element | ShadowRoot
  lightChildren?: Node[]
  capturedLightChildren?: boolean
}
```

将 `mountElementDefinition()` 的签名和相关逻辑改为：

```ts
export function mountElementDefinition(
  ctor: CustomElementConstructor,
  host: HTMLElement,
  initialValues: Map<string, unknown> = new Map(),
  mountState: ElementDefinitionMountState = {},
): MountedElementDefinition {
  const definition = getElementDefinition(ctor)
  const { options, setup, propDefs } = definition
  const propStore = createPropStore(propDefs)

  applyPropDefaults(propStore, propDefs)

  for (const def of propDefs) {
    if (initialValues.has(def.key)) {
      propStore.set(def.key, initialValues.get(def.key))
      continue
    }

    initialValues.set(def.key, propStore.get(def.key))
  }

  const shadow = options.shadow ?? false
  const mode = shadow ? 'shadow' : 'light'

  if (mode === 'light' && !mountState.capturedLightChildren) {
    mountState.lightChildren = Array.from(host.childNodes)
    mountState.capturedLightChildren = true
  }

  const lightChildren = mountState.lightChildren ?? []

  const owner = createOwner()

  for (const context of options.consumes ?? []) {
    const resolved = resolveDOMContext(host, context as Context<unknown>)

    if (resolved.found) {
      owner.provides.set(context.id, resolved.value)
    } else if (context.hasDefaultValue) {
      owner.provides.set(context.id, context.defaultValue)
    }
  }

  const target =
    mountState.target ??
    (mountState.target = resolveExternalRenderTarget(host, shadow))

  const hostContext: HostRenderContext = {
    host,
    mode,
    lightChildren,
  }

  const setupContext: DefineElementContext<HTMLElement> = {
    host,
    emit: (name, detail, eventOptions) => {
      return host.dispatchEvent(
        new CustomEvent(name, {
          bubbles: true,
          composed: true,
          cancelable: true,
          ...eventOptions,
          detail,
        }),
      )
    },
  }

  const dispose = render(
    () =>
      runWithOwner(owner, () =>
        withHostContext(hostContext, () =>
          setup(propStore.props, setupContext),
        ),
      ),
    target,
    { owner },
  )

  mountStyles(target, options.styles)

  return {
    propertyChanged(name, _oldValue, newValue) {
      propStore.set(name, newValue)
    },

    dispose() {
      dispose()
    },
  }
}
```

同时在 `packages/core/runtime-dom/src/index.ts` 中导出类型：

```ts
export type {
  ElementDefinitionMountState,
  MountedElementDefinition,
  ZeusElementDefinition,
} from './defineElement'
```

## 修改 `packages/web-c/output-wc/src/generateLazyEntry.ts`

在生成的组件实例中持久化 mount state：

```ts
`  constructor(hostRef) {`,
`    this.hostRef = hostRef;`,
`    this.mounted = undefined;`,
`    this.mountState = {};`,
`  }`,
```

将 connected 中的 mount 调用改为：

```ts
`    this.mounted = mountElementDefinition(`,
`      ${component.exportName},`,
`      this.hostRef.host,`,
`      this.hostRef.values,`,
`      this.mountState,`,
`    );`,
```

---

# P0：React named slot 名包含 `-` 时会生成非法 JavaScript

当前 React minimal wrapper 会把 slot 名直接放进解构参数：

```ts
const slotDestructure = slotNames.length
  ? `\n    ${slotNames.join(',\n    ')},`
  : ''
```

并且直接用 slot 名拼接局部变量：

```ts
const slotNode_${name} = ${name}
```

如果组件定义了：

```ts
slots: {
  'header-actions': {},
}
```

生成结果会类似：

```ts
function ZCard({
  children,
  header-actions,
  ...rest
}) {}

const slotNode_header-actions = header-actions
```

这是非法 JavaScript。

event-bridge 模式同样会把 props、events 和 slots 直接作为局部变量名解构。

## 修改 `packages/web-c/output-react-wrapper/src/generateReactWrapper.ts`

新增安全 binding 类型和辅助函数：

```ts
interface ReactBinding {
  sourceName: string
  localName: string
}

interface ReactEventBinding extends ReactBinding {
  eventName: string
}

function createBindings(names: string[], prefix: string): ReactBinding[] {
  return names.map((sourceName, index) => ({
    sourceName,
    localName: `${prefix}${index}`,
  }))
}

function generateDestructuredBindings(bindings: ReactBinding[]): string {
  return bindings
    .map(({ sourceName, localName }) => {
      return `${JSON.stringify(sourceName)}: ${localName},`
    })
    .join('\n    ')
}
```

替换 `generateMinimalReactWrapper()`：

```ts
function generateMinimalReactWrapper(
  input: GenerateReactWrapperOptions,
): string {
  const { component, namedSlots, wcModuleId } = input

  const slotBindings = createBindings(
    getNamedSlots(component, namedSlots),
    'slotValue',
  )

  const slotDestructure = slotBindings.length
    ? `\n    ${generateDestructuredBindings(slotBindings)}`
    : ''

  const namedSlotLines = generateMinimalNamedSlots(slotBindings)

  return `
import * as React from 'react';

import ${JSON.stringify(wcModuleId)};

export const ${component.name} = React.forwardRef(
  function ${component.name}({
    children,${slotDestructure}
    ...rest
  } = {}, ref) {
${namedSlotLines}
    return React.createElement(
      ${JSON.stringify(component.tag)},
      {
        ...rest,
        ref,
      },
${namedSlotLines ? '      ...slotNodes,\n' : ''}      children,
    );
  },
);
`.trimStart()
}
```

替换 `generateEventBridgeReactWrapper()` 中的 binding 生成部分：

```ts
function generateEventBridgeReactWrapper(
  input: GenerateReactWrapperOptions,
): string {
  const { component, namedSlots, wcModuleId } = input

  const propBindings = createBindings(Object.keys(component.props), 'propValue')

  const eventBindings: ReactEventBinding[] = Object.keys(component.events).map(
    (eventName, index) => ({
      eventName,
      sourceName: toReactEventProp(eventName),
      localName: `eventHandler${index}`,
    }),
  )

  const slotBindings = createBindings(
    getNamedSlots(component, namedSlots),
    'slotValue',
  )

  const destructuredProps = generateDestructuredBindings([
    ...propBindings,
    ...eventBindings,
    ...slotBindings,
  ])

  return `
import {
  createElement,
  cloneElement,
  Fragment,
  forwardRef,
  isValidElement,
  useEffect,
  useImperativeHandle,
  useRef,
} from 'react';

import ${JSON.stringify(wcModuleId)};

export const ${component.name} = forwardRef(function ${component.name}(props, ref) {
  const {
    children,
    className,
    style,
    ${destructuredProps}
    ...rest
  } = props;

  const innerRef = useRef(null);

  useImperativeHandle(ref, () => innerRef.current);

  ${generatePropSyncLines(propBindings)}

  ${generateEventEffects(eventBindings)}

  const slotChildren = [];

  ${generateNamedSlotRenderLines(slotBindings)}

  if (children != null) {
    slotChildren.push(children);
  }

  return createElement(
    ${JSON.stringify(component.tag)},
    {
      ...rest,
      ref: innerRef,
      className,
      style,
    },
    ...slotChildren,
  );
});

function createNamedSlot(name, value) {
  if (value == null || value === false) return null;

  if (
    isValidElement(value) &&
    value.type !== Fragment
  ) {
    return cloneElement(value, { slot: name });
  }

  return createElement(
    'span',
    {
      slot: name,
      style: { display: 'contents' },
    },
    value,
  );
}
`.trimStart()
}
```

替换相关 helper：

```ts
function generatePropSyncLines(bindings: ReactBinding[]): string {
  if (!bindings.length) {
    return '// no props'
  }

  return `useEffect(() => {
    const el = innerRef.current;
    if (!el) return;

    ${bindings
      .map(({ sourceName, localName }) => {
        return `el[${JSON.stringify(sourceName)}] = ${localName};`
      })
      .join('\n    ')}
  }, [${bindings.map(binding => binding.localName).join(', ')}]);`
}

function generateEventEffects(bindings: ReactEventBinding[]): string {
  return bindings
    .map(({ eventName, localName }) => {
      return `
  useEffect(() => {
    const el = innerRef.current;
    if (!el || !${localName}) return;

    const handler = event => {
      ${localName}(event);
    };

    el.addEventListener(${JSON.stringify(eventName)}, handler);

    return () => {
      el.removeEventListener(${JSON.stringify(eventName)}, handler);
    };
  }, [${localName}]);
`
    })
    .join('')
}

function generateNamedSlotRenderLines(bindings: ReactBinding[]): string {
  return bindings
    .map(({ sourceName, localName }) => {
      return `
  {
    const node = createNamedSlot(${JSON.stringify(sourceName)}, ${localName});
    if (node != null) slotChildren.push(node);
  }
`
    })
    .join('')
}

function generateMinimalNamedSlots(bindings: ReactBinding[]): string {
  if (!bindings.length) return ''

  const lines = bindings.map(({ sourceName, localName }, index) => {
    const nodeName = `slotNode${index}`

    return `    const ${nodeName} = ${localName} != null && ${localName} !== false
      ? (React.isValidElement(${localName}) && ${localName}.type !== React.Fragment
          ? React.cloneElement(${localName}, { slot: ${JSON.stringify(sourceName)} })
          : React.createElement('span', { slot: ${JSON.stringify(sourceName)}, style: { display: 'contents' } }, ${localName}))
      : null;
`
  })

  return (
    lines.join('') +
    '\n    const slotNodes = [' +
    bindings.map((_, index) => `slotNode${index}`).join(', ') +
    '].filter(Boolean);\n'
  )
}
```

React `.d.ts` 当前只生成 props、events、children、className 和 style，没有生成 named slot props。

在 `packages/web-c/component-dts/src/generateReactDts.ts` 中增加：

```ts
for (const name of Object.keys(component.slots)) {
  if (name === 'default') continue

  lines.push(`  ${safePropertyName(name)}?: React.ReactNode`)
}
```

---

# P1：lazy manifest 的 shadow 默认值与 `defineElement` 不一致

lazy manifest 当前默认：

```ts
shadow: ${component.meta?.shadow ?? true}
```

但 `defineElement` 和 `mountElementDefinition` 的默认值都是 `false`。

修改 `packages/web-c/output-wc/src/generateLazyManifest.ts`：

```ts
shadow: ${component.meta?.shadow ?? false},
```

---

# P1：lazy entry 文件名冲突检查没有使用 `entryFileName`

lazy entry 实际文件名来自：

```ts
normalized.entryFileName(component.tag) + '.js'
```

但当前冲突检查仍然只使用 `fileName` 和 `stripPrefix`。

如果用户配置：

```ts
wc({
  entryFileName: () => 'component',
})
```

多个组件会尝试输出同一个 `component.js`。

## 修改 `packages/web-c/output-wc/src/index.ts`

替换 `buildStart()` 中的 collision 检查：

```ts
buildStart(ctx) {
  if (!ctx.manifest) return

  if (normalized.register === 'lazy') {
    if (!normalized.manifest) {
      ctx.error(
        '[zeus-output-wc] register:"lazy" requires manifest:true because the runtime loader needs components.manifest.js.',
      )
    }

    if (!normalized.loader) {
      ctx.error(
        '[zeus-output-wc] register:"lazy" requires loader:true because framework wrappers and auto entry depend on loader.js.',
      )
    }
  }

  checkFileNameCollisions(
    ctx.manifest.components,
    normalized.register === 'lazy'
      ? tag => ensureJsExtension(normalized.entryFileName(tag))
      : tag =>
          normalizeFileName(
            tag,
            normalized.stripPrefix,
            normalized.fileName,
          ),
    {
      warn: ctx.warn,
    },
  )
}
```

替换 collision helper：

```ts
function checkFileNameCollisions(
  components: { tag: string }[],
  getFileName: (tag: string) => string,
  reporter: { warn: (message: string) => void },
): void {
  const map = new Map<string, typeof components>()

  for (const component of components) {
    const fileName = getFileName(component.tag)
    const list = map.get(fileName) ?? []

    list.push(component)
    map.set(fileName, list)
  }

  for (const [fileName, list] of map) {
    if (list.length <= 1) continue

    reporter.warn(
      `[zeus-output-wc] Multiple components map to "${fileName}": ${list
        .map(item => item.tag)
        .join(', ')}`,
    )
  }
}

function ensureJsExtension(fileName: string): string {
  return fileName.endsWith('.js') ? fileName : `${fileName}.js`
}
```

如果还要保留 `warnOnFileNameCollision: false`：

```ts
if (normalized.warnOnFileNameCollision !== false) {
  checkFileNameCollisions(...)
}
```

---

# P1：loader 应在 bootstrap 成功后再标记 registry

当前 loader 在调用 `bootstrapLazy()` 前就执行了：

```ts
definedRegistries.add(registry)
```

如果某个组件 tag 无效，或者 registry.define 中途抛错，该 registry 会永久被标记为已完成，后续无法重试。

修改生成代码：

```ts
export function defineCustomElements(options = {}) {
  const registry =
    options.registry ??
    (typeof customElements !== 'undefined' ? customElements : undefined)

  if (!registry) {
    return
  }

  if (definedRegistries.has(registry)) {
    return
  }

  bootstrapLazy(components, {
    registry,
  })

  definedRegistries.add(registry)
}
```

---

# 必须补的测试

当前真正的 Rollup 集成测试仍然只覆盖 `register: 'side-effect'`，没有覆盖 lazy manifest、动态 import entry、loader 和 auto 入口。

另外，当前测试环境的 `CustomElementRegistry` 只会在 `document.createElement()` 时读取已注册构造器，不会模拟真实浏览器对已有未知元素的 upgrade。
现有“pre-upgrade”测试实际上也是先调用 `bootstrapLazy()`，再创建元素。

至少补下面几类：

```ts
it('preserves light DOM slot children after disconnect and reconnect')
it('reuses closed shadow root after reconnect')
it('generates valid React wrapper for kebab-case named slots')
it('warns when lazy entryFileName values collide')
it('emits and resolves lazy loader chunks via real Rollup build')
```

React named slot 测试：

```ts
it('generates valid code for kebab-case named slots', () => {
  const code = generateReactWrapper({
    component: {
      tag: 'z-card',
      name: 'ZCard',
      exportName: 'ZCard',
      source: 'src/card.tsx',
      props: {},
      events: {},
      slots: {
        default: {},
        'header-actions': {},
      },
      hostAttributes: [],
      cssParts: [],
      cssVars: [],
    },
    namedSlots: 'props',
    wcModuleId: 'zeus:wc:z-card',
    mode: 'minimal',
  })

  expect(code).toContain('"header-actions": slotValue0')
  expect(code).not.toContain('slotNode_header-actions')
  expect(code).not.toContain('header-actions,')
})
```

---

## 最终评价

```txt
设计方向：正确
上一轮问题：已基本修复
当前完成度：约 92%
是否建议合并：暂不建议
```

建议修复顺序：

```txt
1. 持久化 lazy mount state，修复重连、light slot、closed shadow root
2. 修复 React named slot 安全标识符
3. 修复 shadow 默认值
4. 修复 lazy entry 文件名冲突检查
5. 增加真实 Rollup lazy integration test
6. rebase main，并将无关依赖升级拆出当前功能分支
```

当前分支相对 `main` 已经出现分叉，且包含 Rollup、Rolldown、TypeScript preview 等依赖升级改动；建议在合并前单独处理这些无关变更。
