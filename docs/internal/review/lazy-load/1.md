## 结论

本次审查基于最新 head `51219e8888268848fc11e3310031bf994c992df9`。

**暂不建议合并。** 当前 Stencil-style lazy loader 主流程已经比较完整，上一轮的 manifest default、Vue 显式 prop 同步、React 18 peer 范围、必需 loader/manifest 等问题已经修复。

剩余完成度约 **97%**，但还有两个会直接造成 lazy 产物错误的 P0，以及几个需要合并前处理的发布与一致性问题。

---

# P0：lazy manifest 使用了合并后的公开 props，而不是原始 runtime props

`ComponentRecord.props` 是 runtime props、TypeScript 类型 props、meta props 的合并结果。

但 lazy manifest 直接遍历 `component.props`，把全部公开 props 注册成 Proxy Element property 和 observed attribute。

真实 `defineElement` 只根据 `options.props` 创建 `propDefs` 和 prop store。

例如：

```ts
interface ButtonProps {
  label?: string
  disabled?: boolean
}

export const ZButton = defineElement<ButtonProps>(
  'z-button',
  {
    props: {
      disabled: Boolean,
    },
  },
  props => <button disabled={props.disabled}>{props.label}</button>,
)
```

当前 lazy manifest 会把 `label` 也注册成 runtime property，但真实组件的 prop store 中没有 `label`。用户写入 `element.label` 后，Proxy 能读到值，组件 setup 却读不到值。

## 推荐修改：在 ComponentRecord 中保留原始 runtime props

### `packages/web-c/component-analyzer/src/types.ts`

```ts
export interface ComponentRecord {
  tag: string
  name: string
  exportName: string
  source: string

  /**
   * Public props merged from runtime definitions, TypeScript types and metadata.
   * Used by declarations, documentation and framework wrapper types.
   */
  props: Record<string, ComponentProp>

  /**
   * Exact runtime props extracted from defineElement(options.props).
   * Lazy Proxy Elements must only consume this field.
   */
  runtimeProps?: Record<string, ComponentProp>

  /**
   * Validation messages explaining why runtime props could not be analyzed.
   */
  runtimePropsDiagnostics?: string[]

  events: Record<string, ComponentEvent>
  slots: Record<string, ComponentSlot>

  hostAttributes: string[]
  cssParts: string[]
  cssVars: string[]

  description?: string
  meta?: {
    shadow?: boolean
    [key: string]: unknown
  }
}
```

### `packages/web-c/component-analyzer/src/merge.ts`

修改 `BuildRecordOptions`：

```ts
export interface BuildRecordOptions {
  file: string
  call: DefineElementCallRecord
  runtimeProps: Record<string, ComponentProp>
  runtimePropsDiagnostics?: string[]
  typeProps: Record<string, Partial<ComponentProp>>
  setupMeta: SetupMeta
  inlineMeta: InlineMeta
  shadow?: boolean
}
```

修改 `buildComponentRecord()`：

```ts
export function buildComponentRecord(
  options: BuildRecordOptions,
): ComponentRecord {
  const {
    file,
    call,
    runtimeProps,
    runtimePropsDiagnostics,
    typeProps,
    setupMeta,
    inlineMeta,
    shadow,
  } = options

  const props = mergeProps(
    runtimeProps,
    typeProps,
    inlineMeta.props as Record<string, Partial<ComponentProp>> | undefined,
  )

  const events = mergeEvents(
    setupMeta.events,
    inlineMeta.events as Record<string, ComponentEvent> | undefined,
  )

  const slots = mergeSlots(
    setupMeta.slots,
    inlineMeta.slots as Record<string, ComponentSlot> | undefined,
  )

  const cssParts = unique([
    ...setupMeta.cssParts,
    ...toStringArray(inlineMeta.cssParts),
  ])

  const cssVars = unique(toStringArray(inlineMeta.cssVars))
  const hostAttributes = unique(setupMeta.hostAttributes)
  const restMeta = stripKnownMetaFields(inlineMeta)

  return {
    tag: call.tag,
    name: call.name,
    exportName: call.exportName,
    source: file,

    props,
    runtimeProps,
    runtimePropsDiagnostics: runtimePropsDiagnostics?.length
      ? runtimePropsDiagnostics
      : undefined,

    events,
    slots,
    hostAttributes,
    cssParts,
    cssVars,

    description:
      typeof inlineMeta.description === 'string'
        ? inlineMeta.description
        : undefined,

    meta:
      shadow !== undefined || restMeta ? { ...restMeta, shadow } : undefined,
  }
}
```

### 完整替换 `packages/web-c/output-wc/src/generateLazyManifest.ts`

```ts
import type {
  ComponentProp,
  ComponentRecord,
} from '@zeus-js/component-analyzer'

export interface GenerateLazyManifestOptions {
  components: ComponentRecord[]
  getEntryFileName: (tag: string) => string
  tagPrefix?: string
}

export function generateLazyManifest(
  options: GenerateLazyManifestOptions,
): string {
  const { components, getEntryFileName } = options

  const componentEntries = components.map(component => {
    const entryFile = getEntryFileName(component.tag).replace(/\\/g, '/')
    const runtimeProps = component.runtimeProps ?? component.props
    const props = generatePropsArray(runtimeProps)

    const importPath = entryFile.startsWith('.')
      ? JSON.stringify(entryFile)
      : JSON.stringify(`./${entryFile}`)

    return `  {
    tagName: ${JSON.stringify(component.tag)},
    shadow: ${component.meta?.shadow ?? false},
    load: () => import(${importPath}),
    props: ${props},
  }`
  })

  return `export const components = [
${componentEntries.join(',\n')}
];
`
}

function generatePropsArray(props: Record<string, ComponentProp>): string {
  const entries = Object.entries(props)

  if (entries.length === 0) {
    return '[]'
  }

  const lines = entries.map(([name, prop]) => {
    const parts: string[] = [`name: ${JSON.stringify(name)}`]

    if (prop.attr === false) {
      parts.push('attrName: false')
    } else {
      const attrName = prop.attr ?? toKebabCase(name)

      if (attrName !== toKebabCase(name)) {
        parts.push(`attrName: ${JSON.stringify(attrName)}`)
      }
    }

    parts.push(`type: ${JSON.stringify(prop.type)}`)

    if (prop.reflect) {
      parts.push('reflect: true')
    }

    return `      { ${parts.join(', ')} }`
  })

  return `[
${lines.join(',\n')}
    ]`
}

function toKebabCase(value: string): string {
  return value.replace(/[A-Z]/g, match => `-${match.toLowerCase()}`)
}
```

### 补充测试

```ts
it('only emits runtime props into the lazy manifest', () => {
  const code = generateLazyManifest({
    components: [
      {
        tag: 'z-button',
        name: 'ZButton',
        exportName: 'ZButton',
        source: 'src/button.tsx',

        props: {
          label: {
            type: 'string',
          },
          disabled: {
            type: 'boolean',
          },
        },

        runtimeProps: {
          disabled: {
            type: 'boolean',
          },
        },

        events: {},
        slots: {},
        hostAttributes: [],
        cssParts: [],
        cssVars: [],
      },
    ],

    getEntryFileName: tag => `${tag}.entry.js`,
  })

  expect(code).toContain('name: "disabled"')
  expect(code).not.toContain('name: "label"')
})
```

---

# P0：options 顶层 spread 可以绕过静态 props 校验

新加入的校验会检查 `props` 对象内部的 spread、method 和 computed key。

但它没有检查 options 对象本身。`getObjectProperty()` 会忽略 spread，因此下面代码会通过校验，最终仍生成空 runtime props。

```ts
const baseOptions = {
  props: {
    disabled: Boolean,
  },
}

export const ZButton = defineElement(
  'z-button',
  {
    ...baseOptions,
  },
  () => null,
)
```

## 修改 `validateRuntimePropsDefinition()`

在 `if (!options) return messages` 之后增加：

```ts
for (const member of options.properties) {
  if (!t.isObjectProperty(member) || member.computed) {
    messages.push(
      'defineElement() options cannot contain spreads, methods, or computed keys in component output builds.',
    )
  }
}
```

完整位置：

```ts
export function validateRuntimePropsDefinition(
  options: t.ObjectExpression | undefined,
  optionsArgument: t.Node | undefined,
): string[] {
  const messages: string[] = []

  if (optionsArgument && !t.isObjectExpression(optionsArgument)) {
    messages.push(
      'defineElement() options must be an inline object literal so component metadata can be analyzed.',
    )

    return messages
  }

  if (!options) {
    return messages
  }

  for (const member of options.properties) {
    if (!t.isObjectProperty(member) || member.computed) {
      messages.push(
        'defineElement() options cannot contain spreads, methods, or computed keys in component output builds.',
      )
    }
  }

  const propsNode = getObjectProperty(options, 'props')

  if (!propsNode) {
    return messages
  }

  // 保留后续现有校验逻辑
}
```

### 补充测试

```ts
it('reports an error when defineElement options contain spreads', () => {
  const result = analyzeFile({
    file: 'button.tsx',

    code: `
      import { defineElement } from '@zeus-js/runtime-dom'

      const baseOptions = {
        props: {
          disabled: Boolean,
        },
      }

      export const ZButton = defineElement(
        'z-button',
        {
          ...baseOptions,
        },
        () => null,
      )
    `,
  })

  expect(result.diagnostics).toContainEqual(
    expect.objectContaining({
      level: 'error',
      message: expect.stringContaining('options cannot contain spreads'),
    }),
  )
})
```

---

# P1：静态 props 校验全局阻断了 `side-effect` 兼容模式

当前 `analyzeFile()` 把无法静态分析的 runtime props 直接记录为 `error`。

bundler plugin 会在所有 output plugin 执行前终止任何 analyzer error。

但 `register: "side-effect"` 的公开说明仍然是兼容旧行为。

这意味着即使用户只需要 side-effect Web Component 输出、不需要 lazy manifest，原本可以运行的动态 options 组件也会构建失败。

## 推荐方案

Analyzer 将该问题记录为 warning 和组件级 diagnostics；只有 lazy output 强制报错。

### 修改 `packages/web-c/component-analyzer/src/analyzeFile.ts`

```ts
for (const call of calls) {
  const runtimePropsDiagnostics = validateRuntimePropsDefinition(
    call.options,
    call.call.arguments[1],
  )

  for (const message of runtimePropsDiagnostics) {
    diagnostics.push({
      level: 'warning',
      file,
      message: `<${call.tag}> ${message}`,
    })
  }

  const runtimeProps = extractRuntimeProps(call.options)
  const shadowOption = extractShadowOption(call.options)

  // 其余现有逻辑保持不变

  components.push(
    buildComponentRecord({
      file,
      call,
      runtimeProps,
      runtimePropsDiagnostics,
      typeProps,
      setupMeta,
      inlineMeta,
      shadow:
        shadowOption.shadow ??
        (typeof inlineMeta.shadow === 'boolean'
          ? inlineMeta.shadow
          : undefined),
    }),
  )
}
```

### 修改 `packages/web-c/output-wc/src/index.ts`

在 `buildStart()` 中增加 lazy 专属校验：

```ts
buildStart(ctx) {
  if (!ctx.manifest) return

  if (normalized.register === 'lazy') {
    for (const component of ctx.manifest.components) {
      const diagnostics = component.runtimePropsDiagnostics ?? []

      if (diagnostics.length === 0) {
        continue
      }

      ctx.error(
        [
          `[zeus-output-wc] <${component.tag}> cannot be emitted with register:"lazy" because its runtime props are not statically analyzable.`,
          ...diagnostics.map(message => `- ${message}`),
        ].join('\n'),
      )
    }
  }

  checkFileNameCollisions(
    ctx.manifest.components,
    {
      getFileName: tag => {
        if (normalized.register === 'lazy') {
          return getLazyEntryFileName(normalized.entryFileName, tag)
        }

        return normalizeFileName(
          tag,
          normalized.stripPrefix,
          normalized.fileName,
        )
      },

      warnOnFileNameCollision: normalized.warnOnFileNameCollision,
    },
    {
      warn: ctx.warn,
    },
  )
}
```

如果你希望所有 component output 都强制静态 props，也可以保留全局 error，但需要删除 `side-effect` 的“Compatible with legacy behavior”说明，并在文档中明确这是组件库编译语法限制。

---

# P1：Vue `event-bridge` 不应该复制 Web Component default

Vue event-bridge 当前仍会把分析到的 default 写入 Vue prop option。

wrapper 已经通过 raw vnode props 判断用户是否显式传值。

因此 Vue wrapper 不需要持有默认值。复制默认值会产生两个问题：

```txt
1. Object / Array default 不是 factory，会产生 Vue 警告和共享状态。
2. 用户显式传入 undefined 时，Vue 可能先替换为 wrapper default，
   导致 Web Component 收不到真正的 undefined。
```

## 替换 `toVuePropOption()`

```ts
function toVuePropOption(prop: ComponentRecord['props'][string]): string {
  const typeMap: Record<string, string> = {
    string: 'String',
    number: 'Number',
    boolean: 'Boolean',
    object: 'Object',
    array: 'Array',
    unknown: 'null',
  }

  const type = typeMap[prop.type] ?? 'null'
  const required = prop.required === true ? 'true' : 'false'

  return `{ type: ${type}, required: ${required} }`
}
```

补充测试：

```ts
it('does not copy Web Component defaults into Vue wrapper props', () => {
  const code = generateVueWrapper({
    component: {
      tag: 'z-button',
      name: 'ZButton',
      exportName: 'ZButton',
      source: 'src/button.tsx',

      props: {
        config: {
          type: 'object',
          default: {
            size: 'md',
          },
        },
      },

      events: {},
      slots: {},
      hostAttributes: [],
      cssParts: [],
      cssVars: [],
    },

    wcModuleId: 'zeus:wc:z-button',
    mode: 'event-bridge',
  })

  expect(code).not.toContain('default:')
})
```

---

# P1：lazy entry 固定导入 `@zeus-js/runtime-dom`，但 output-wc 未声明该依赖

生成的 lazy entry 会直接导入：

```ts
import { mountElementDefinition } from '@zeus-js/runtime-dom'
```

但 `@zeus-js/output-wc` 目前只新增了 `@zeus-js/web-c-runtime` 依赖，没有新增 `@zeus-js/runtime-dom`。

在 pnpm 严格依赖布局中，用户组件源码可能只直接安装并导入 `@zeus-js/zeus`，虚拟 lazy entry 的 `@zeus-js/runtime-dom` 裸导入不一定能够解析。

## 最小修改

`packages/web-c/output-wc/package.json`：

```json
{
  "dependencies": {
    "@zeus-js/bundler-plugin": "workspace:*",
    "@zeus-js/component-analyzer": "workspace:*",
    "@zeus-js/component-dts": "workspace:*",
    "@zeus-js/runtime-dom": "workspace:*",
    "@zeus-js/web-c-runtime": "workspace:*"
  }
}
```

更完整的长期方案是记录组件源码中 `defineElement` 的实际导入来源，并让 lazy entry 从相同包导入 `mountElementDefinition`。

---

# P1：changeset 缺少 `@zeus-js/runtime-dom`

分支已经从 `@zeus-js/runtime-dom` 新增导出：

```txt
getElementDefinition
mountElementDefinition
ZEUS_ELEMENT_DEFINITION
MountedElementDefinition
ElementDefinitionMountState
NormalizedPropDefinition
ZeusElementConstructor
ZeusElementDefinition
```

但最新 changeset 列出的包中没有 `@zeus-js/runtime-dom`。

changeset 应增加：

```yaml
---
'@zeus-js/component-analyzer': patch
'@zeus-js/output-react-wrapper': patch
'@zeus-js/output-vue-wrapper': patch
'@zeus-js/output-wc': minor
'@zeus-js/preset-component-library': minor
'@zeus-js/runtime-dom': minor
'@zeus-js/web-c-runtime': patch
---
```

---

# P2：`web-c-runtime` 的 shadow 默认值与 `defineElement` 不一致

`ZeusLazyComponentMeta.shadow` 当前文档写的是默认 `true`。

`web-c-runtime` 也只有在 `shadow === false` 时使用 light DOM。

但 `defineElement` 的默认值是 `false`。

## 修改

`packages/web-c/web-c-runtime/src/types.ts`：

```ts
/**
 * Whether to render into a ShadowRoot.
 *
 * @default false
 */
shadow?: boolean
```

`packages/web-c/web-c-runtime/src/lifecycle.ts`：

```ts
function getRenderRoot(hostRef: HostRef): ShadowRoot | HTMLElement {
  if (!hostRef.meta.shadow) {
    return hostRef.host
  }

  return (
    hostRef.host.shadowRoot ??
    hostRef.host.attachShadow({
      mode: 'open',
    })
  )
}
```

---

# 其他建议

`componentLibrary()` 允许用户配置任意 targets，但 React 和 Vue wrapper 都明确要求 `wc()` plugin。

建议在 preset 中自动补充 `wc`：

```ts
const targets = new Set(options.targets ?? ['wc', 'react', 'vue'])

if (targets.has('react') || targets.has('vue')) {
  targets.add('wc')
}
```

Vue wrapper 文档中的 event-bridge 说明仍写成了 React CustomEvent bridging，需要改成 Vue。

PR 当前仍显示 `mergeable: false`，标题和描述也只覆盖了早期的 shadow analyzer 改动，没有反映 59 个文件、7371 行新增的 lazy loader 改造。

建议 PR 标题改为：

```txt
feat(web-c): add Stencil-style lazy loader and minimal framework wrappers
```

---

## 最终评价

```txt
设计方向：正确
lazy loader 主流程：基本完成
当前完成度：约 97%
是否建议合并：暂不建议
```

合并前至少完成：

```txt
1. lazy manifest 只消费原始 runtime props
2. 补齐 options 顶层 spread 校验
3. 决定静态 props 校验是否只约束 lazy 模式
4. 移除 Vue wrapper default
5. 补 @zeus-js/runtime-dom 依赖与 changeset
```

我尝试拉取分支运行本地定向测试，但当前执行环境无法解析 GitHub 域名，因此本轮无法独立验证测试结果。
