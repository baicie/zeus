结论：**可以合并到主线，MVP 已完成。**

我这次重新看的是最新 `feat/signal`，当前分支相对 `main` 是 **ahead 242、behind 0**，Git 层面可以干净合并。上次剩下的问题也基本处理完了：`jsx-runtime` 的 types 已经指向 `.d.ts`，`create-zeus` 已明确排除在 MVP release precheck 外，复杂 example 也已经加入 examples 检查。

# 当前判断

```txt
技术 MVP：完成
主线合并：可以合并
alpha 发布：核心包可以准备，create-zeus 暂不发布
```

建议合并前只做一件事：

```bash
pnpm release:precheck
```

这个脚本现在会跑 `build / build-dts / check / lint / test-unit / examples:check:all / docs:build / size:ci / check:exports`，已经可以作为合并前主闸门。

---

# 已满足 MVP 的核心点

## 1. 主入口 API 已收敛

`@zeus-js/zeus` 当前只导出用户 API，包括：

```txt
state / computed / effect / watch / scope
batch / untrack / nextTick / onCleanup
render / Show / For / Host / Slot / defineElement
createContext / useContext / provide / inject
```

没有再把 `template / insert / bindText / marker` 这类 runtime internal helper 暴露到主入口。这个 API 面已经适合作为 MVP 主入口。

## 2. 包出口已修正

`@zeus-js/zeus` 的 `jsx-runtime` 和 `jsx-dev-runtime` 子路径现在已经正确指向 `.d.ts`：

```json
"./jsx-runtime": {
  "types": "./jsx-runtime.d.ts",
  "default": "./jsx-runtime.js"
}
```

这个问题上次是小阻断，现在已修。

## 3. create-zeus 的发布策略已明确

`release-precheck.ts` 明确写了：`create-zeus` 被排除在 MVP precheck 外，因为它是 experimental DX tool，并且如果未来发布，需要加入 `create-zeus:build` 并从 changeset ignore 移除。这个策略清晰，可以接受。

## 4. examples 覆盖已经足够支撑 MVP

现在 `check-examples.ts` 覆盖了：

```txt
counter
todo
web-component
context
light-dom-slots
project-board
```

其中 `project-board` 已经纳入检查。

复杂 example `project-board` 覆盖了 `For / Show / computed / createContext / defineElement / Host / Slot / useContext` 等核心 API。 它还包含 Web Component `z-task-inspector`，并通过 `consumes: [BoardContext]` 消费 Context，能验证 Context bridge + Web Component 的 MVP 能力。

## 5. 表单绑定和事件语义也被复杂 example 覆盖

`project-board` 里使用了：

```tsx
prop:value={title.value}
prop:checked={urgent.value}
event.currentTarget
```

这能验证 `prop:` property binding 和 delegated event 的 `currentTarget` 语义。

---

# 仍建议合并后补的 P1 项

这些不是合并阻断，但建议合并后尽快做。

## P1-1：examples 检查目前只跑 `check`，没有跑 `build`

~~`check-examples.ts` 现在只执行：~~

~~```ts
pnpm -F <example> check
```~~

~~没有执行 `build`。~~

**已升级为 check + build 两阶段。**

现在 `check-examples.ts` 对每个 example 串行执行：

```ts
pnpm -F <example> check   // tsc --noEmit
pnpm -F <example> build   // vite build
```

每个 example 的 check 失败会立即中止；check 通过后才会执行 build。

**注意**：packages 必须先 build（因为 examples 依赖 `workspace:*` 的 dist/）。在 `release-precheck` 里 build 已在第一步跑过；独立运行时需手动先 `pnpm build` 再跑此脚本。

## P1-2：`release:dry` 现在只是跑 precheck

~~根脚本里：~~

~~```json
"release:dry": "pnpm release:precheck"
```~~

~~它不是实际的 changeset dry-run。~~

**已升级。**

```json
"release:dry": "pnpm release:precheck && changeset status && changeset version --snapshot test-dry-run"
```

流程：precheck 全量验证 → changeset status 预览待发布 changeset → changeset version dry-run 模拟版本计算（snapshot 不实际写文件）。

## P1-3：create-zeus 暂时不要作为 v0.1.0-alpha 卖点

虽然根脚本里已经有 `create-zeus:build`，但 release precheck 明确排除了它。

所以首版 alpha 推荐只发核心包，不主打 `pnpm create zeus`。

---

# MVP 是否完成

按之前定的标准：

```txt
1. state() 可用
2. TSX 能通过 Vite plugin 编译
3. compiler 走 direct DOM path
4. render / binding / event / ref 可用
5. Show / For 可用
6. Context / Web Component 可用
7. examples 能覆盖核心场景
8. docs / test / release precheck 有基础闭环
9. API 主入口收敛
```

现在可以认为：**完成。**

尤其新增的 `project-board` 复杂 example 已经把多个关键能力串起来：

```txt
state + computed
For + Show
Context Provider bridge
Web Component consumes
Slot / Host
prop:value / prop:checked
事件委托 currentTarget
```

这已经足够证明 Zeus 的技术 MVP 闭环成立。

---

# 合并建议

可以合并：

```bash
pnpm release:precheck
git checkout main
git merge feat/signal
```

建议合并提交信息：

```txt
feat: complete zeus technical mvp
```

或者 PR 标题：

```txt
feat: complete compiler-first Zeus MVP
```

最终判断：

```txt
可以合并到 main。
MVP 已完成。
create-zeus 暂不算入 MVP 发布范围。
```

合并后下一步建议 ~~是：补 examples build 检查、完善 release dry-run，然后准备 `v0.1.0-alpha`~~：**examples build 检查和 release dry-run 均已补完**，可直接准备 `v0.1.0-alpha`。`create-zeus` 暂不纳入 MVP 发布范围。
