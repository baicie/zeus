下面这版按你的两个前提重新收口：

```txt
1. @zeus-ui/headless 暂时不是主线发布包，而是 compiler-host 的接近实战用例。
   所以可以继续放在 examples/headless。

2. 主任务是 Web Component 编译器宿主能力。
   插件形态应该围绕一个主 zeus bundler plugin，再通过 plugins 属性挂 wc/react/vue/icons 等输出插件。
```

我建议这次迭代目标从“继续加功能”改成：

```txt
Component Compiler Host Plugin Architecture Stabilization
```

也就是把当前 `outputs` 架构升级成更清晰的 `plugins` / `componentHost` 架构，同时保留向后兼容。

---

# 1. 当前实现重新评价

之前我把 `@zeus-ui/headless` 放在 `examples/headless` 视为 P0，是因为我按“它要作为正式发布包”来判断。按你现在的定位，**它放在 `examples/headless` 是可以接受的**。

当前 `examples/headless/package.json` 的名字是 `@zeus-ui/headless`，并且 `private: true`，这符合“模拟真实组件库包名，但不发布”的实验定位。
它也确实暴露了接近真实包的 exports，包括 `wc/react/vue/styles.css/custom-elements.json/zeus.components.json`，这对验证 compiler-host 输出链路很有价值。

所以新的判断是：

```txt
@zeus-ui/headless 保留 examples/headless：可以
不要求移到 packages/headless：可以
但必须明确它是 example fixture，不参与发布
use-headless-react/vue/cli 这类 consumer example 要纳入 examples 检查
```

---

# 2. 你提的插件架构建议

你现在想要的方向可以整理成两种 API：

## 方案 A：主 zeus plugin + plugins 属性

```ts
import zeus from '@zeus-js/bundler-plugin/vite'
import wc from '@zeus-js/output-wc'
import react from '@zeus-js/output-react-wrapper'
import vue from '@zeus-js/output-vue-wrapper'

export default {
  plugins: [
    zeus({
      components: {
        include: ['src/**/*.{ts,tsx}'],
      },

      plugins: [wc(), react(), vue()],
    }),
  ],
}
```

这个最直观，适合普通用户。

---

## 方案 B：独立 componentHost 容器 + zeus 适配

```ts
import zeus, { componentHost } from '@zeus-js/bundler-plugin/vite'
import wc from '@zeus-js/output-wc'
import react from '@zeus-js/output-react-wrapper'
import vue from '@zeus-js/output-vue-wrapper'

const host = componentHost({
  components: {
    include: ['src/**/*.{ts,tsx}'],
  },

  output: {
    wcDir: 'wc',
    reactDir: 'react',
    vueDir: 'vue',
    dts: true,
    stripPrefix: false,
  },

  plugins: [wc(), react(), vue()],
})

export default {
  plugins: [zeus(host)],
}
```

这个更适合后续扩展 Rollup / Vite / Rolldown，因为 `componentHost()` 本身和 bundler 无关。

---

# 3. 推荐采用方案

我建议两个都支持，但内部统一成 **ComponentHostConfig**：

```txt
推荐用户 API：
zeus({
  components,
  plugins: [wc(), react(), vue()]
})

高级用户 API：
zeus(componentHost({
  components,
  output,
  plugins: [wc(), react(), vue()]
}))
```

同时保留当前的 `outputs` 作为兼容 alias：

```ts
zeus({
  outputs: [wc(), react(), vue()],
})
```

当前实现里 `createZeusPlugin()` 已经是通过 `options.outputs` 驱动 output plugin，在 buildStart 阶段调用 `virtualModules()` 并 emit chunk，在 generateBundle 阶段调用各 output plugin 的 `generateBundle()`。
所以这次改造不需要推翻当前实现，只需要把命名和上下文抽象做稳。

---

# 4. 新架构目标

这次改造要解决 4 个问题：

```txt
1. outputs 命名不够通用，后续 react/vue/icons 都更像 plugins。
2. wcOutDir / fileName / stripPrefix 在各 wrapper 中容易配置不一致。
3. @zeus-ui/headless 作为 example fixture 需要被正确 build/check。
4. CI 应该验证 compiler-host 主链路，而不是只跑 build/check/test。
```

---

# 5. 新类型设计

## `packages/web-c/bundler-plugin/src/types.ts`

```ts
import type { OutputBundle, PluginContext } from 'rollup'
import type { ComponentManifest } from '@zeus-js/component-analyzer'

export interface ZeusBundlerPluginOptions {
  root?: RootOption

  components?: {
    include?: string[]
    exclude?: string[]
  }

  compiler?: ZeusCompilerOptions

  diagnostics?: boolean

  /**
   * New preferred API.
   */
  plugins?: ZeusComponentPlugin[]

  /**
   * Backward compatibility.
   *
   * @deprecated Use `plugins` instead.
   */
  outputs?: ZeusComponentPlugin[]

  /**
   * Shared output config for all component plugins.
   */
  output?: ZeusComponentOutputConfig
}

export type RootOption = string | (() => string)

export interface ZeusComponentOutputConfig {
  /**
   * Root output dir inside bundler output dir.
   *
   * Usually empty. Keep each plugin using wc/react/vue.
   */
  outDir?: string

  /**
   * Directory for Web Component output.
   *
   * @default 'wc'
   */
  wcDir?: string

  /**
   * Directory for React wrapper output.
   *
   * @default 'react'
   */
  reactDir?: string

  /**
   * Directory for Vue wrapper output.
   *
   * @default 'vue'
   */
  vueDir?: string

  /**
   * Directory for icons output.
   *
   * @default 'icons'
   */
  iconsDir?: string

  /**
   * Shared filename naming rule.
   */
  fileName?: (tag: string, kind: ZeusComponentOutputKind) => string

  /**
   * Shared stripPrefix rule.
   */
  stripPrefix?: string | false

  /**
   * Shared dts switch.
   */
  dts?: boolean
}

export type ZeusComponentOutputKind =
  | 'wc'
  | 'react'
  | 'vue'
  | 'icons-react'
  | 'icons-vue'
  | 'icons-wc'
  | 'asset'

export interface ZeusComponentHostConfig {
  root?: RootOption
  components?: ZeusBundlerPluginOptions['components']
  compiler?: ZeusBundlerPluginOptions['compiler']
  diagnostics?: boolean
  output?: ZeusComponentOutputConfig
  plugins: ZeusComponentPlugin[]
}

export interface ZeusBuildContext {
  root: string
  manifest: ComponentManifest
  diagnostics: ZeusDiagnostic[]

  output: RequiredZeusComponentOutputConfig
  paths: ZeusOutputPathResolver

  emitFile: PluginContext['emitFile']
  warn: PluginContext['warn']
  error: PluginContext['error']
  addWatchFile: PluginContext['addWatchFile']

  meta: {
    watchMode: boolean
  }
}

export interface RequiredZeusComponentOutputConfig {
  outDir: string
  wcDir: string
  reactDir: string
  vueDir: string
  iconsDir: string
  stripPrefix: string | false
  dts: boolean
  fileName?: (tag: string, kind: ZeusComponentOutputKind) => string
}

export interface ZeusOutputPathResolver {
  getFileName(tag: string, kind: ZeusComponentOutputKind): string

  getDir(kind: ZeusComponentOutputKind): string

  join(kind: ZeusComponentOutputKind, fileName: string): string

  relativeImport(
    from: ZeusComponentOutputKind,
    to: ZeusComponentOutputKind,
    tag: string,
  ): string
}

export interface ZeusComponentPlugin {
  name: string

  buildStart?(ctx: ZeusBuildContext): void | Promise<void>

  virtualModules?(
    ctx: ZeusBuildContext,
  ): ZeusVirtualModule[] | Promise<ZeusVirtualModule[]>

  generateBundle?(
    ctx: ZeusBuildContext,
    bundle: OutputBundle,
  ): ZeusOutputFile[] | Promise<ZeusOutputFile[]>
}

export interface ZeusVirtualModule {
  id: string
  code: string
  fileName?: string
}

export type ZeusOutputFile =
  | {
      type: 'asset'
      fileName: string
      source: string | Uint8Array
    }
  | {
      type: 'chunk'
      id: string
      fileName: string
    }
```

核心变化：

```txt
ZeusOutputPlugin -> ZeusComponentPlugin
outputs -> plugins
新增 output 公共配置
新增 ctx.paths 统一解决 wc/react/vue 文件路径
```

---

# 6. componentHost 容器

## `packages/web-c/bundler-plugin/src/componentHost.ts`

```ts
import type {
  ZeusBundlerPluginOptions,
  ZeusComponentHostConfig,
  ZeusComponentPlugin,
} from './types'

export function componentHost(
  config: ZeusComponentHostConfig,
): ZeusBundlerPluginOptions {
  return {
    root: config.root,
    components: config.components,
    compiler: config.compiler,
    diagnostics: config.diagnostics,
    output: config.output,
    plugins: config.plugins,
  }
}

export function resolveComponentPlugins(
  options: ZeusBundlerPluginOptions,
): ZeusComponentPlugin[] {
  const plugins = options.plugins ?? options.outputs ?? []

  if (options.outputs && !options.plugins) {
    // Do not hard warn in CI by default. Keep it soft for migration.
    // Could be enabled by diagnostics later.
  }

  return plugins
}
```

## `packages/web-c/bundler-plugin/src/index.ts`

```ts
export { createZeusPlugin } from './rollup'
export { createZeusVitePlugin, zeus } from './vite'
export { componentHost } from './componentHost'

export type {
  ZeusBundlerPluginOptions,
  ZeusComponentHostConfig,
  ZeusComponentPlugin,
  ZeusBuildContext,
  ZeusVirtualModule,
  ZeusOutputFile,
  ZeusComponentOutputConfig,
  ZeusComponentOutputKind,
} from './types'
```

---

# 7. 统一路径解析器

这是这次设计的关键。React/Vue wrapper 不应该自己猜 WC 文件路径。

当前 React wrapper 是通过 `wcOutDir` 拼相对路径，并且 fileName 由 wrapper 自己计算。
`wcOutDir` 的默认值也在 React wrapper 内部单独定义。

这容易导致 wc 和 react 的 `stripPrefix/fileName` 不一致。

## `packages/web-c/bundler-plugin/src/outputPaths.ts`

```ts
import path from 'node:path'

import type {
  RequiredZeusComponentOutputConfig,
  ZeusComponentOutputConfig,
  ZeusComponentOutputKind,
  ZeusOutputPathResolver,
} from './types'

export function normalizeOutputConfig(
  config: ZeusComponentOutputConfig | undefined,
): RequiredZeusComponentOutputConfig {
  return {
    outDir: config?.outDir ?? '',
    wcDir: config?.wcDir ?? 'wc',
    reactDir: config?.reactDir ?? 'react',
    vueDir: config?.vueDir ?? 'vue',
    iconsDir: config?.iconsDir ?? 'icons',
    stripPrefix: config?.stripPrefix ?? false,
    dts: config?.dts ?? true,
    fileName: config?.fileName,
  }
}

export function createOutputPathResolver(
  output: RequiredZeusComponentOutputConfig,
): ZeusOutputPathResolver {
  return {
    getFileName(tag, kind) {
      if (output.fileName) {
        return output.fileName(tag, kind)
      }

      return `${normalizeTagToFileName(tag, output.stripPrefix)}.js`
    },

    getDir(kind) {
      const dir = getKindDir(kind, output)
      return output.outDir ? path.posix.join(output.outDir, dir) : dir
    },

    join(kind, fileName) {
      return path.posix.join(this.getDir(kind), fileName)
    },

    relativeImport(from, to, tag) {
      const fromDir = this.getDir(from)
      const toDir = this.getDir(to)
      const toFile = path.posix.join(toDir, this.getFileName(tag, to))

      let relative = path.posix.relative(fromDir, toFile)

      if (!relative.startsWith('.')) {
        relative = `./${relative}`
      }

      return relative
    },
  }
}

function getKindDir(
  kind: ZeusComponentOutputKind,
  output: RequiredZeusComponentOutputConfig,
): string {
  switch (kind) {
    case 'wc':
      return output.wcDir
    case 'react':
      return output.reactDir
    case 'vue':
      return output.vueDir
    case 'icons-react':
      return path.posix.join(output.iconsDir, 'react')
    case 'icons-vue':
      return path.posix.join(output.iconsDir, 'vue')
    case 'icons-wc':
      return path.posix.join(output.iconsDir, 'wc')
    case 'asset':
      return output.outDir
  }
}

function normalizeTagToFileName(
  tag: string,
  stripPrefix: string | false,
): string {
  let name = tag

  if (stripPrefix && name.startsWith(stripPrefix)) {
    name = name.slice(stripPrefix.length)
  }

  return name
}
```

---

# 8. Rollup plugin 改造

## `packages/web-c/bundler-plugin/src/rollup.ts`

核心改动：把 `outputs` 改成 `plugins`，并挂载 `ctx.output / ctx.paths`。

```ts
import path from 'node:path'

import { analyzeComponents } from '@zeus-js/component-analyzer'
import fg from 'fast-glob'

import { formatDiagnostic, hasErrorDiagnostics } from './diagnostics'
import { createFilter } from './filter'
import { transformZeus } from './transform'
import { VirtualModuleRegistry } from './virtual'
import { resolveComponentPlugins } from './componentHost'
import { createOutputPathResolver, normalizeOutputConfig } from './outputPaths'

import type {
  RootOption,
  ZeusBuildContext,
  ZeusBundlerPluginOptions,
  ZeusOutputFile,
  ZeusVirtualModule,
} from './types'
import type { ComponentManifest } from '@zeus-js/component-analyzer'
import type { Plugin } from 'rollup'

export function createZeusPlugin(
  options: ZeusBundlerPluginOptions = {},
): Plugin {
  const shouldTransform = createFilter(options)
  const virtualModules = new VirtualModuleRegistry()

  let ctx: ZeusBuildContext | undefined

  return {
    name: 'zeus-bundler-plugin',

    async buildStart() {
      virtualModules.clear()

      const root = resolveRoot(options.root)
      const manifestResult = await createManifest(root, options)

      for (const file of await collectWatchFiles(root, options)) {
        this.addWatchFile(file)
      }

      const diagnostics = manifestResult.diagnostics

      for (const diagnostic of diagnostics) {
        const message = formatDiagnostic(diagnostic)

        if (diagnostic.level === 'error') {
          this.error(message)
        } else if (options.diagnostics !== false) {
          this.warn(message)
        }
      }

      if (hasErrorDiagnostics(diagnostics)) {
        this.error('[zeus] component analyzer failed.')
      }

      const output = normalizeOutputConfig(options.output)
      const paths = createOutputPathResolver(output)

      ctx = {
        root,
        manifest: manifestResult.manifest,
        diagnostics,
        output,
        paths,
        emitFile: this.emitFile.bind(this),
        warn: this.warn.bind(this),
        error: this.error.bind(this),
        addWatchFile: this.addWatchFile.bind(this),
        meta: {
          watchMode: this.meta.watchMode,
        },
      }

      const plugins = resolveComponentPlugins(options)

      for (const plugin of plugins) {
        await plugin.buildStart?.(ctx)
      }

      for (const plugin of plugins) {
        const modules = await plugin.virtualModules?.(ctx)

        if (!modules) continue

        for (const mod of modules) {
          virtualModules.set(mod.id, mod.code, mod.fileName)
        }

        emitComponentEntries(modules, this)
      }
    },

    resolveId(id, importer) {
      if (ctx) {
        const resolved = resolveSourceFile(id, importer, ctx.root, ctx.manifest)
        if (resolved) {
          return resolved
        }
      }

      const resolvedVirtualModule = virtualModules.resolve(id, importer)

      if (resolvedVirtualModule) {
        return {
          id: resolvedVirtualModule,
          moduleSideEffects: 'no-treeshake',
        }
      }

      return null
    },

    load(id) {
      return virtualModules.load(id)
    },

    async transform(code, id) {
      if (!shouldTransform(id)) {
        return null
      }

      const result = await transformZeus({
        id,
        code,
        compiler: options.compiler,
        sourcemap: true,
      })

      return result ?? null
    },

    async generateBundle(_, bundle) {
      if (!ctx) return

      const plugins = resolveComponentPlugins(options)

      for (const plugin of plugins) {
        const files = await plugin.generateBundle?.(ctx, bundle)

        if (!files) continue

        for (const file of files) {
          emitOutputFile(this, file)
        }
      }
    },
  }
}
```

保留当前已有的 `resolveSourceFile()`、`emitOutputFile()`、`emitComponentEntries()` 即可。

---

# 9. Vite API

## `packages/web-c/bundler-plugin/src/vite.ts`

保留现状，但导出 `componentHost`。

```ts
import { createRequire } from 'node:module'
import path from 'node:path'

import { componentHost } from './componentHost'
import { createZeusPlugin } from './rollup'

import type { ZeusBundlerPluginOptions, ZeusComponentHostConfig } from './types'
import type { Plugin, UserConfig, ResolvedConfig } from 'vite'

export function createZeusVitePlugin(
  options: ZeusBundlerPluginOptions | ZeusComponentHostConfig = {},
): Plugin {
  let resolvedConfig: ResolvedConfig | undefined

  const rollupPlugin = createZeusPlugin({
    ...options,
    root: () => resolvedConfig?.root ?? process.cwd(),
  }) as Plugin

  return {
    ...rollupPlugin,
    name: 'vite-plugin-zeus',
    enforce: 'pre',

    async config(userConfig) {
      const runtimeDomEntry = resolveRuntimeDOMEntry(userConfig.root)

      return {
        ...((await isRolldownVite())
          ? {
              oxc: {
                jsx: 'preserve',
              },
            }
          : {
              esbuild: {
                jsx: 'preserve',
              },
            }),

        resolve: {
          alias: runtimeDomEntry
            ? {
                '@zeus-js/runtime-dom': runtimeDomEntry,
              }
            : undefined,
          dedupe: ['@zeus-js/signal', '@zeus-js/runtime-dom', '@zeus-js/zeus'],
        },
      } satisfies UserConfig
    },

    configResolved(config) {
      resolvedConfig = config

      if (typeof rollupPlugin.configResolved === 'function') {
        return rollupPlugin.configResolved.call(this, config)
      }
    },
  }
}

export default createZeusVitePlugin
export { createZeusVitePlugin as zeus, componentHost }
```

---

# 10. output-wc 改造

## 使用新 ctx.paths

```ts
export default function wc(options: OutputWCOptions = {}): ZeusComponentPlugin {
  const normalized = normalizeOptions(options)

  return {
    name: 'zeus-output-wc',

    buildStart(ctx) {
      checkFileNameCollisions(ctx.manifest.components, ctx, normalized, {
        warn: ctx.warn,
      })
    },

    virtualModules(ctx) {
      const modules: ZeusVirtualModule[] = []

      for (const component of ctx.manifest.components) {
        const fileName = ctx.paths.join(
          'wc',
          ctx.paths.getFileName(component.tag, 'wc'),
        )

        modules.push({
          id: getVirtualComponentId(component),
          fileName,
          code: generateWCEntry({
            root: ctx.root,
            component,
          }),
        })
      }

      if (normalized.index) {
        modules.push({
          id: getVirtualIndexId(),
          fileName: ctx.paths.join('wc', 'index.js'),
          code: generateWCIndex({
            components: ctx.manifest.components,
            getFileName: tag => ctx.paths.getFileName(tag, 'wc'),
          }),
        })
      }

      return modules
    },

    generateBundle(ctx) {
      const files: ZeusOutputFile[] = []

      if (normalized.manifestFile) {
        files.push({
          type: 'asset',
          fileName: normalized.manifestFile,
          source: generateZeusComponentsManifest(ctx.manifest),
        })
      }

      if (normalized.customElementsFile) {
        files.push({
          type: 'asset',
          fileName: normalized.customElementsFile,
          source: generateCustomElementsJson({
            manifest: ctx.manifest,
            getModulePath: component =>
              ctx.paths.join('wc', ctx.paths.getFileName(component.tag, 'wc')),
          }),
        })
      }

      if (normalized.dts || normalized.jsxDts) {
        const dtsFiles = generateWCDtsFiles(ctx.manifest, {
          outDir: ctx.output.wcDir,
          stripPrefix: ctx.output.stripPrefix,
          fileName: tag => ctx.paths.getFileName(tag, 'wc'),
          perComponent: true,
          index: normalized.dts,
          jsx: normalized.jsxDts,
        })

        for (const file of dtsFiles) {
          files.push({
            type: 'asset',
            fileName: file.fileName,
            source: file.source,
          })
        }
      }

      return files
    },
  }
}
```

`output-wc` 自己还可以保留 `outDir`，但建议逐步迁移到公共 `output.wcDir`。

---

# 11. React wrapper 改造

## 现有问题

React wrapper 现在自己计算 WC import：

```ts
const wcFileName = getWcFileName(component.tag)
const wcImport = `${options.wcOutDir}/${wcFileName}`
```

这在 wc/react 配置不一致时容易错。

## 新设计

让 `generateReactWrapper()` 直接接收 `wcImport`。

```ts
export interface GenerateReactWrapperOptions {
  component: ComponentRecord
  options: RequiredOutputReactWrapperOptions
  wcImport: string
}
```

## `generateReactWrapper.ts`

```ts
export function generateReactWrapper(
  input: GenerateReactWrapperOptions,
): string {
  const { component, options, wcImport } = input
  const propNames = Object.keys(component.props)
  const eventNames = Object.keys(component.events)
  const namedSlots = getNamedSlots(component, options)

  return `
import React, {
  forwardRef,
  useEffect,
  useImperativeHandle,
  useRef,
} from 'react';

// Import and register the Web Component.
import { ${component.exportName} as __zeusWC } from ${JSON.stringify(wcImport)};

if (typeof customElements !== 'undefined' && !customElements.get(${JSON.stringify(component.tag)})) {
  void __zeusWC;
}

export const ${component.name} = forwardRef(function ${component.name}(props, ref) {
  const {
    children,
    className,
    style,
    ${createDestructureNames(component, namedSlots)}
    ...rest
  } = props;

  const innerRef = useRef(null);

  useImperativeHandle(ref, () => innerRef.current);

  ${generatePropSyncLines(propNames)}

  ${generateEventEffects(eventNames)}

  const slotChildren = [];

  ${generateNamedSlotRenderLines(namedSlots)}

  if (children != null) {
    slotChildren.push(children);
  }

  return React.createElement(
    ${JSON.stringify(component.tag)},
    {
      ...rest,
      ref: innerRef,
      className,
      style,
    },
    ...slotChildren,
  );
});
`.trimStart()
}
```

## `output-react-wrapper/src/index.ts`

```ts
export default function reactWrapper(
  options: OutputReactWrapperOptions = {},
): ZeusComponentPlugin {
  const normalized = normalizeOptions(options)

  return {
    name: 'zeus-output-react-wrapper',

    virtualModules(ctx) {
      const modules: ZeusVirtualModule[] = []

      for (const component of ctx.manifest.components) {
        const fileName = ctx.paths.join(
          'react',
          ctx.paths.getFileName(component.tag, 'react'),
        )

        modules.push({
          id: `zeus:react:${component.tag}`,
          fileName,
          code: generateReactWrapper({
            component,
            options: normalized,
            wcImport: ctx.paths.relativeImport('react', 'wc', component.tag),
          }),
        })
      }

      if (normalized.index) {
        modules.push({
          id: 'zeus:react:index',
          fileName: ctx.paths.join('react', 'index.js'),
          code: generateReactIndex(ctx.manifest.components, {
            ...normalized,
            getFileName: tag => ctx.paths.getFileName(tag, 'react'),
          }),
        })
      }

      return modules
    },

    generateBundle(ctx) {
      if (!normalized.dts) return []

      return [
        {
          type: 'asset',
          fileName: ctx.paths.join('react', 'index.d.ts'),
          source: generateReactDts(ctx.manifest),
        },
      ]
    },
  }
}
```

这样 `wc` 和 `react` 文件命名完全由 `ctx.paths` 统一控制。

---

# 12. Vue wrapper 同步修正

当前 Vue wrapper 已经修了裸变量问题，但同步逻辑还有一个边界：所有 props 都是 `undefined` 时，外层 `if` 不执行，就不会清理旧值。

建议生成无条件同步：

```ts
function generateVuePropSyncLines(propNames: string): string {
  if (propNames.length === 0) {
    return '// no props to sync'
  }

  return propNames.map(name => `el.${name} = props.${name};`).join('\n      ')
}
```

生成结果：

```ts
const syncProps = () => {
  const el = elRef.value
  if (!el) return

  el.variant = props.variant
  el.disabled = props.disabled
}
```

这比当前 `if props.xxx !== undefined` 更简单，也更符合 Vue wrapper 的受控组件语义。

---

# 13. 用户 API 最终形态

## 13.1 最简单用法

```ts
import zeus from '@zeus-js/bundler-plugin/vite'
import wc from '@zeus-js/output-wc'
import react from '@zeus-js/output-react-wrapper'
import vue from '@zeus-js/output-vue-wrapper'

export default {
  plugins: [
    zeus({
      components: {
        include: ['src/components/**/*.{ts,tsx}'],
      },

      plugins: [wc(), react(), vue()],
    }),
  ],
}
```

---

## 13.2 通用 output 配置

```ts
export default {
  plugins: [
    zeus({
      components: {
        include: ['src/components/**/*.{ts,tsx}'],
      },

      output: {
        wcDir: 'wc',
        reactDir: 'react',
        vueDir: 'vue',
        stripPrefix: 'z-',
        dts: true,
      },

      plugins: [wc(), react(), vue()],
    }),
  ],
}
```

---

## 13.3 高级容器用法

```ts
import zeus, { componentHost } from '@zeus-js/bundler-plugin/vite'
import wc from '@zeus-js/output-wc'
import react from '@zeus-js/output-react-wrapper'
import vue from '@zeus-js/output-vue-wrapper'

const host = componentHost({
  components: {
    include: ['src/components/**/*.{ts,tsx}'],
  },

  output: {
    wcDir: 'wc',
    reactDir: 'react',
    vueDir: 'vue',
    stripPrefix: false,
  },

  plugins: [
    wc({
      index: true,
      customElementsFile: 'custom-elements.json',
      manifestFile: 'zeus.components.json',
    }),

    react({
      namedSlots: 'props',
    }),

    vue({
      globalDts: true,
    }),
  ],
})

export default {
  plugins: [zeus(host)],
}
```

---

# 14. examples/headless 的定位与改造

保留：

```txt
examples/headless
```

并明确写在文档里：

```txt
examples/headless is a real-world fixture package.
It uses the package name @zeus-ui/headless to validate real package exports,
but it is private and not part of the publishable packages.
```

## `examples/headless/package.json`

保留：

```json
{
  "name": "@zeus-ui/headless",
  "private": true
}
```

但建议补充：

```json
{
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "prepack": "echo \"@zeus-ui/headless is an example fixture and should not be published\" && exit 1"
  }
}
```

---

# 15. examples 检查脚本改造

当前 `check-examples.ts` 没有覆盖所有新增 headless consumer examples。

建议改成显式分层：

## `scripts/check/check-examples.ts`

```ts
import { exec } from '../shared/utils'

const buildFirst = ['@zeus-ui/headless']

const examples = [
  '@zeus-js/example-counter',
  '@zeus-js/example-todo',
  '@zeus-js/example-web-component',
  '@zeus-js/example-react-wrapper',
  '@zeus-js/example-vue-wrapper',
  '@zeus-js/example-registry-react',
  '@zeus-js/example-registry-vue',
  '@zeus-js/example-icons-no-runtime',
  '@zeus-js/example-context',
  '@zeus-js/example-light-dom-slots',
  '@zeus-js/example-project-board',

  '@zeus-js/example-headless-demo',
  '@zeus-js/example-use-headless-react',
  '@zeus-js/example-use-headless-vue',
  '@zeus-js/example-use-headless-cli',
]

const phases: Array<[string, string]> = [
  ['check', 'type-check'],
  ['build', 'build'],
]

async function run() {
  if (process.argv.includes('--build-first')) {
    console.log('\n> pnpm build\n')
    await exec('pnpm', ['build'], { stdio: 'inherit' })
  }

  for (const name of buildFirst) {
    console.log(`\n> [fixture build] ${name}\n`)
    await exec('pnpm', ['-F', name, 'check'], { stdio: 'inherit' })
    await exec('pnpm', ['-F', name, 'build'], { stdio: 'inherit' })
  }

  for (const name of examples) {
    for (const [cmd, label] of phases) {
      console.log(`\n> [${label}] ${name}\n`)
      await exec('pnpm', ['-F', name, cmd], { stdio: 'inherit' })
    }
  }

  console.log(`\nAll examples passed check + build.\n`)
}

run().catch(err => {
  console.error(err)
  process.exit(1)
})
```

这样 `@zeus-ui/headless` 作为 fixture 会先构建，consumer examples 再构建。

---

# 16. CI 调整建议

当前 `component-host-rc.yml` fast job 只跑 build/check/test，bench job 只跑 benchmark 和 release notes。

建议加一个 `integration` job：

```yaml
integration:
  runs-on: ubuntu-latest

  steps:
    - uses: actions/checkout@v5

    - uses: pnpm/action-setup@v4
      with:
        version: 10.33.4

    - uses: actions/setup-node@v5
      with:
        node-version-file: '.node-version'
        cache: 'pnpm'

    - run: pnpm install --frozen-lockfile

    - run: pnpm build

    - name: Check package exports
      run: pnpm check:exports

    - name: Check examples
      run: pnpm examples:check:all

    - name: Check registry CLI
      run: pnpm check:registry-cli
```

---

# 17. output-icons 修复建议

当前 React icon generator 生成 `<svg dangerouslySetInnerHTML=...>title children</svg>`，这在 React 中有运行时问题。

建议修成：

```ts
export function generateReactIcon(icon: NormalizedIconSource): string {
  const inner = escapeTemplateLiteral(icon.innerSvg)

  return `
import React from 'react';

export const ${icon.componentName} = React.forwardRef(function ${icon.componentName}(props, ref) {
  const {
    size = '1em',
    title,
    children,
    ...rest
  } = props;

  const content = children != null
    ? children
    : React.createElement('g', {
        dangerouslySetInnerHTML: { __html: \`${inner}\` },
      });

  return React.createElement(
    'svg',
    {
      ...rest,
      ref,
      width: size,
      height: size,
      viewBox: ${JSON.stringify(icon.viewBox)},
      fill: 'none',
      xmlns: 'http://www.w3.org/2000/svg',
      'aria-hidden': title ? undefined : true,
      role: title ? 'img' : undefined,
    },
    title ? React.createElement('title', null, title) : null,
    content,
  );
});
`.trimStart()
}
```

Static WC icon 目前是通过 `innerHTML` 渲染 SVG，`sanitizeSvg()` 会移除 `<script>` 和 `onxxx` 属性，MVP 可以接受。
后续再考虑更完整的 SVG AST sanitizer。

---

# 18. DialogTrigger 示例修正

当前 `use-headless-react` example 里用了：

```tsx
<ZDialogTrigger asChild>
  <button>Open Dialog</button>
</ZDialogTrigger>
```

但底层 `ZDialogTrigger` 固定渲染内部 `<button>`，没有 `asChild` 支持。

因为 `@zeus-ui/headless` 当前只是 example fixture，我建议先不做 asChild，直接改 example：

```tsx
<ZDialogTrigger>Open Dialog</ZDialogTrigger>
```

或者 registry 层自己封装 button，不把 `asChild` 暴露出来。

---

# 19. 迁移策略

这次不要一次性删除 `outputs`，建议按三步走。

## Step 1：同时支持 outputs 和 plugins

```ts
const plugins = options.plugins ?? options.outputs ?? []
```

文档只写 `plugins`。

---

## Step 2：给 outputs 加 deprecated 注释

```ts
/**
 * @deprecated Use `plugins` instead.
 */
outputs?: ZeusComponentPlugin[]
```

---

## Step 3：后续版本移除 outputs

等主线稳定后再移除。

---

# 20. 推荐最终配置示例

## `examples/headless/vite.config.ts`

```ts
import { defineConfig } from 'vite'

import zeus, { componentHost } from '@zeus-js/bundler-plugin/vite'
import wc from '@zeus-js/output-wc'
import react from '@zeus-js/output-react-wrapper'
import vue from '@zeus-js/output-vue-wrapper'

export default defineConfig({
  plugins: [
    zeus(
      componentHost({
        components: {
          include: ['src/**/*.{ts,tsx}'],
          exclude: ['src/shared/**'],
        },

        output: {
          wcDir: 'wc',
          reactDir: 'react',
          vueDir: 'vue',
          dts: true,
          stripPrefix: false,
        },

        plugins: [
          wc({
            manifestFile: 'zeus.components.json',
            customElementsFile: 'custom-elements.json',
            jsxDts: true,
          }),

          react({
            namedSlots: 'props',
          }),

          vue({
            globalDts: true,
          }),
        ],
      }),
    ),
  ],

  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
})
```

## 简化写法也支持

```ts
export default defineConfig({
  plugins: [
    zeus({
      components: {
        include: ['src/**/*.{ts,tsx}'],
      },

      output: {
        wcDir: 'wc',
        reactDir: 'react',
        vueDir: 'vue',
      },

      plugins: [wc(), react(), vue()],
    }),
  ],
})
```

---

# 21. 当前合 main 前的新 checklist

> **本阶段暂不实施，留待后续迭代。** 以下列出的改造计划仅供参考，暂不执行。
> 主要原因是当前阶段优先稳定 core plugin 架构，不在本次合 main 前引入过多下游联动变更。

按你的定位，之前的 checklist 要调整成：

```txt
必须修：
[ ] outputs -> plugins 兼容改造
[ ] 新增 componentHost 容器
[ ] 新增 ctx.output / ctx.paths
[ ] React/Vue wrapper 使用 ctx.paths 生成 WC import
[ ] Vue prop sync 改成无条件同步，避免 undefined 清理失败
[ ] output-icons React dangerouslySetInnerHTML 修复
[ ] use-headless-react 删除 asChild 示例
[ ] examples:check:all 纳入所有 headless consumer examples
[ ] CI 增加 check:exports / examples:check:all / check:registry-cli

可以保留：
[x] @zeus-ui/headless 放 examples/headless
[x] @zeus-ui/headless private
[x] use-headless-react/vue 作为真实消费示例
```

---

# 22. 推荐提交顺序

```bash
# 1. plugin types + componentHost container
git add packages/web-c/bundler-plugin/src/types.ts \
        packages/web-c/bundler-plugin/src/componentHost.ts \
        packages/web-c/bundler-plugin/src/index.ts
git commit -m "feat(bundler-plugin): add component host plugin container"

# 2. shared output path resolver
git add packages/web-c/bundler-plugin/src/outputPaths.ts
git commit -m "feat(bundler-plugin): add shared output path resolver"

# 3. rollup/vite plugin migration
git add packages/web-c/bundler-plugin/src/rollup.ts \
        packages/web-c/bundler-plugin/src/vite.ts
git commit -m "feat(bundler-plugin): support component plugins option"

# 4. wc/react/vue output plugins use ctx.paths
git add packages/web-c/output-wc \
        packages/web-c/output-react-wrapper \
        packages/web-c/output-vue-wrapper
git commit -m "refactor(web-c): resolve wrapper imports from host paths"

# 5. examples/headless config migrate
git add examples/headless/vite.config.ts
git commit -m "example(headless): use component host plugin container"

# 6. fix icon generator
git add packages/web-c/output-icons/src/generateReactIcon.ts
git commit -m "fix(output-icons): avoid svg innerHTML children conflict"

# 7. fix examples and checks
git add examples/use-headless-react scripts/check/check-examples.ts .github/workflows/component-host-rc.yml
git commit -m "test(component-host): cover headless consumer examples"
```

---

# 23. 最终结论

你的思路是对的：

```txt
@zeus-ui/headless 不必急着变成正式包。
它现在更适合作为 compiler-host 的实战 fixture。

主线更应该优先稳定：
zeus bundler plugin
componentHost 容器
plugins 扩展点
wc/react/vue/icons 输出插件
统一输出路径和类型生成
```

这版设计之后，整体架构会变成：

```txt
zeus() bundler plugin
  ↓
componentHost config
  ↓
component-analyzer
  ↓
ComponentManifest
  ↓
plugins:
  - output-wc
  - output-react-wrapper
  - output-vue-wrapper
  - output-icons
  - future output-svelte / output-solid / output-lit
```

而 `examples/headless` 的角色是：

```txt
真实组件库 fixture
验证：
  wc output
  react/vue wrapper
  dts
  custom-elements.json
  registry/CLI 消费
  benchmark
```

这样主任务不会被 UI 组件库绑架，同时又有足够真实的用例验证 compiler-host 是否可用。
